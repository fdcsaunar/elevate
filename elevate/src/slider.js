jQuery(function($) {

    $(window).on('load', function() {
      // The slider being synced must be initialized first
      $('#slideshow .slick').slick({
        autoplay: true
    });
     
    });
  
  
  
  
  });